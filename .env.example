# PostgreSQL
POSTGRES_DB=cineva
POSTGRES_USER=cineva
POSTGRES_PASSWORD=changeme

# Full connection URL (used by Drizzle + Fastify)
DATABASE_URL=postgres://cineva:changeme@postgres:5432/cineva

# API
PORT=3000
CORS_ORIGIN=http://localhost

# Client (Vite exposes VITE_* vars to browser)
VITE_API_BASE=/api
